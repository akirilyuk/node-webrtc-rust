name: Build & Publish

on:
  push:
    branches: [main, initial-feature]
    tags: ['release/*']
  pull_request:
    branches: [main, initial-feature]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-apple-darwin
            os: macos-latest
            build-args: --target x86_64-apple-darwin

          - target: aarch64-apple-darwin
            os: macos-latest
            build-args: --target aarch64-apple-darwin

          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            build-args: --target x86_64-unknown-linux-gnu
            cross: true

          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            build-args: --target x86_64-unknown-linux-musl
            cross: true

          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            build-args: --target aarch64-unknown-linux-gnu
            cross: true

          - target: x86_64-pc-windows-msvc
            os: windows-latest
            build-args: --target x86_64-pc-windows-msvc

    name: Build - ${{ matrix.target }}
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install zig for cross-compilation
        if: matrix.cross
        uses: mlugg/setup-zig@v2
        with:
          version: 0.14.1

      - name: Install bindings dependencies
        working-directory: packages/bindings
        run: npm ci --ignore-scripts

      - name: Build native addon
        working-directory: packages/bindings
        run: |
          EXTRA_FLAGS=""
          if [ "${{ matrix.cross }}" = "true" ]; then
            EXTRA_FLAGS="--zig"
          fi
          npx napi build --platform --release ${{ matrix.build-args }} $EXTRA_FLAGS

      - name: Upload bindings artifact
        uses: actions/upload-artifact@v4
        with:
          name: bindings-${{ matrix.target }}
          path: packages/bindings/*.node
          if-no-files-found: error

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: build

    services:
      coturn:
        image: coturn/coturn:latest
        ports:
          - 3478:3478/udp
          - 3478:3478/tcp
        options: >-
          --entrypoint turnserver
          turnserver -n --log-file=stdout
          --min-port=49152 --max-port=65535
          --realm=test.local
          --user=testuser:testpass
          --no-tls --no-dtls
          --listening-port=3478

    env:
      TURN_AVAILABLE: '1'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: dtolnay/rust-toolchain@stable

      - name: Download native binding
        uses: actions/download-artifact@v4
        with:
          name: bindings-x86_64-unknown-linux-gnu
          path: packages/bindings/

      - run: npm ci
      - run: npm run format:check
      - run: npm run lint
      - run: npm run typecheck
      - run: cargo test -p node-webrtc-rust-core
      - run: cargo test -p node-webrtc-rust-mixer
      - run: cargo test -p node-webrtc-rust-conference
      - run: npm run build:ts
      - run: npm test

  publish-dryrun:
    name: Publish (dry-run)
    runs-on: ubuntu-latest
    needs: [build, test]
    if: github.event_name == 'pull_request'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - run: npm ci --ignore-scripts

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: ci-artifacts

      - name: Stage binding artifacts
        working-directory: packages/bindings
        run: |
          mkdir -p artifacts
          shopt -s nullglob
          for dir in ../../ci-artifacts/bindings-*; do
            cp -r "$dir" "artifacts/$(basename "$dir")"
          done
          npx napi artifacts --dir artifacts

      - name: Build TypeScript packages
        run: npm run build:ts

      - run: npm publish --dry-run --workspaces

  publish:
    name: Publish release
    runs-on: ubuntu-latest
    needs: [build, test]
    if: startsWith(github.ref, 'refs/tags/release/')

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - name: Resolve release version from tag
        id: version
        run: |
          VERSION="${GITHUB_REF_NAME#release/}"
          if [ -z "$VERSION" ]; then
            echo "Could not parse version from tag: $GITHUB_REF_NAME" >&2
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Publishing release version: $VERSION"

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: ci-artifacts

      - name: Stage binding artifacts
        working-directory: packages/bindings
        run: |
          mkdir -p artifacts
          shopt -s nullglob
          for dir in ../../ci-artifacts/bindings-*; do
            cp -r "$dir" "artifacts/$(basename "$dir")"
          done
          npx napi artifacts --dir artifacts

      - name: Set workspace package versions
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          for ws in \
            @node-webrtc-rust/bindings \
            @node-webrtc-rust/sdk \
            @node-webrtc-rust/signaling; do
            npm version "$VERSION" --no-git-tag-version --workspace="$ws"
          done

      - name: Build TypeScript packages
        run: npm run build:ts

      - name: Prepare binding packages
        working-directory: packages/bindings
        run: npx napi prepublish -t npm

      - name: Publish npm packages
        run: |
          for dir in packages/bindings/npm/*/; do
            if [ -f "${dir}package.json" ]; then
              npm publish "$dir" --access public
            fi
          done
          npm publish packages/bindings --access public
          npm publish --workspace=@node-webrtc-rust/sdk --access public
          npm publish --workspace=@node-webrtc-rust/signaling --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ steps.version.outputs.version }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
